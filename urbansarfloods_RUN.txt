# UrbanSARFloods pipeline (run from repo root, inside the OlmoEarth venv: `source env_olmo.sh`)
# HF rate-limits anonymous downloads -> set a token first: `hf auth login` (or export HF_TOKEN=...)

# 1. Download train/val tar (~38GB) + inspect. Skip the 414GB raw SLC.
HF_HUB_DISABLE_XET=1 hf download S1Floodbenchmark/UrbanSARFloods_v1 urban_sar_floods.tar.gz --repo-type dataset --local-dir data

# 2. Extract (-> data/urban_sar_floods/ with 01_NF 02_FO 03_FU + Train/Valid_dataset.txt)
tar xzf data/urban_sar_floods.tar.gz -C data

# 3. Tile 512->tile_size sub-tiles, flood-only, drop NaN-nodata tiles (-> data/urbansarfloods_tiles_t64/)
#    change --tile_size 32 to try 32x32 (writes _t32; must divide 512)
python -u prep_urbansarfloods_tiles.py --splits train,valid --tile_size 64

# 4. Extract frozen OlmoEarth features (patch_size=4, input_res=20, keeps T=2 pre/post).
#    --tile_size must match step 3; feature folder -> features/usf_base_s1_ps4_res20_t64/
python -u extract_urbansarfloods_features.py --splits train,valid --tile_size 64

# 5. Linear-probe: concat pre+post features -> per-pixel head; reports val(=test) mIoU(NF,FO)
python -u lp_urbansarfloods.py --features usf_base_s1_ps4_res20_t64 --weighted_ce
